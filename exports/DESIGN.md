# Halderworks Instrument - DESIGN.md

Generated by `tools/export.py` from `tokens.json`. Do not hand-edit.

A house design system: one token set, 2 themes, two densities. 33 colour tokens per theme, all pairs certified at WCAG AA for text and 3:1 for non-text indicators. Neutral ground, one accent hue, colour spent only where a state is reported.

## Rules that are not negotiable

1. Never write a literal colour, size, radius, duration, breakpoint or z-index. A value the system lacks is a gap to report, not a number to invent.
2. Put ink only on a ground the permission table allows. Never re-derive a ratio by eye.
3. The primary action is hw-ink and carries no hue. There is exactly one per screen.
4. The accent has four jobs: a link, a selected row, a live state, the focus ring. A screen with no state on it has no hue on it.
5. Every state carries a word, never a colour alone.
6. No gradient behind text, no backdrop-filter, no shadow on anything that cannot be dismissed. A patterned ground carries only ink certified against its own worst pixel.
7. Uppercase exists at one step, micro, for a column head or an eyebrow.
8. Every column of figures takes tabular-nums.
9. Left-aligned by default. Centre only a single-element empty state or a dialog action row.
10. The focus ring is never removed.
11. No invented quotes, logos, metrics or placeholder data presented as real.

## Tokens

### color

| token | light | dark | role |
|---|---|---|---|
| `--hw-ground` | `oklch(0.978 0.003 198)` | `oklch(0.152 0.006 198)` | The page or window background. Every screen starts here. |
| `--hw-surface` | `oklch(1.000 0 198)` | `oklch(0.196 0.006 198)` | Cards, panels and table bodies: one step off the ground. |
| `--hw-surface-hover` | `oklch(0.972 0.003 198)` | `oklch(0.215 0.006 198)` | The hover fill of a row, a menu item or a quiet button. 1.08:1 off surface in light, 1.04:1 in dark: a step the eye finds and the layout does not. |
| `--hw-surface-active` | `oklch(0.958 0.003 198)` | `oklch(0.232 0.006 198)` | The pressed fill, and the skeleton fill while content loads. One step past hover. |
| `--hw-surface-raised` | `oklch(1.000 0 198)` | `oklch(0.232 0.006 198)` | Things that float over content and can be dismissed: dialogs, popovers, menus. In light theme this is the same white as surface on purpose - what separates them there is the shadow. In dark theme it is a real lightness step, because a shadow on a dark ground is invisible. |
| `--hw-surface-sunken` | `oklch(0.958 0.003 198)` | `oklch(0.118 0.006 198)` | Wells that recede: sidebars, code blocks, an input at rest. |
| `--hw-border` | `oklch(0.912 0.0045 198)` | `oklch(0.288 0.0084 198)` | The 1px hairline: card and panel edges, table and cell borders, the tab baseline, chart axes and gridlines, the masthead rule, a framed quotation. Separates a container from its ground without being read as an edge. |
| `--hw-border-strong` | `oklch(0.6350 0.0060 198)` | `oklch(0.5179 0.0084 198)` | Control outlines that must be seen: inputs, selects, textareas, checkboxes, switches, slider rails, secondary buttons, the selected segment of a segmented control, and a floating panel's edge. Held to WCAG 1.4.11's 3:1 as a control boundary: 3.01:1 at worst in both themes. The first two passes shipped it at 1.64:1 light and 1.92:1 dark on hw-surface. |
| `--hw-text` | `oklch(0.254 0.0060 198)` | `oklch(0.938 0.0072 198)` | Primary text. 14.83:1 on ground, 15.78:1 on surface, 13.98:1 on surface-sunken. |
| `--hw-text-secondary` | `oklch(0.471 0.0060 198)` | `oklch(0.687 0.0072 198)` | Supporting text, descriptions, metadata. 6.37:1 on ground. |
| `--hw-text-muted` | `oklch(0.533 0.0060 198)` | `oklch(0.619 0.0072 198)` | Column heads, placeholders, timestamps. 4.88:1 on ground, the floor of this system. |
| `--hw-text-disabled` | `oklch(0.6350 0.006 198)` | `oklch(0.516 0.0072 198)` | Text and icons in a disabled control. Held to the 3:1 non-text bar, not to AA, and solved per theme because one opacity cannot mean one thing in both: hw-text at 45% composites to 1.73:1 in light and 7.40:1 in dark. |
| `--hw-ink` | `oklch(0.205 0.0060 198)` | `oklch(0.955 0.0030 198)` | The primary action fill: one filled button per screen, and the selected segment of a segmented control. Carries no hue so the accent stays scarce. |
| `--hw-ink-hover` | `oklch(0.290 0.0060 198)` | `oklch(0.870 0.0030 198)` | Hover state of a primary action. |
| `--hw-ink-active` | `oklch(0.360 0.0060 198)` | `oklch(0.800 0.0030 198)` | The pressed fill of a primary button. One step past hover in the same direction, so a press is distinguishable from a hover on the one control that commits an action. |
| `--hw-ink-text` | `oklch(0.995 0 198)` | `oklch(0.145 0.006 198)` | Text and icons on an ink fill. 17.63:1 on ink. |
| `--hw-accent` | `oklch(0.515 0.079 198)` | `oklch(0.619 0.096 198)` | The one brand hue. Links, selected state, live state, focus. 5.09:1 on ground. |
| `--hw-accent-hover` | `oklch(0.465 0.072 198)` | `oklch(0.679 0.096 198)` | Hover state of an accent link or accent fill. |
| `--hw-accent-quiet` | `oklch(0.94 0.035 198)` | `oklch(0.244 0.035 198)` | A flat accent tint for a selected row or an accent badge. Solved so hw-accent clears AA on it, which the first pass asserted but never checked. |
| `--hw-accent-ring` | `oklch(0.625 0.079 198)` | `oklch(0.512 0.074 198)` | The focus ring, at 2px with a 2px offset. Held to the WCAG 2.2 non-text bar of 3:1 against the surface CLOSEST to it in lightness, not against the ground: solved against the ground alone it measured 2.56:1 on hw-surface-raised in dark theme. |
| `--hw-success` | `oklch(0.51 0.123 152)` | `oklch(0.618 0.11 152)` | Passed, verified, settled: a badge, a validation summary, a positive delta against a baseline. Never a brand colour. 5.10:1 on ground. |
| `--hw-success-quiet` | `oklch(0.94 0.035 152)` | `oklch(0.244 0.035 152)` | Fill behind success text in a badge or banner. Solved so hw-success clears AA on it, which the first pass asserted but never checked. |
| `--hw-warning` | `oklch(0.527 0.101 78)` | `oklch(0.634 0.11 78)` | Retried, degraded, expiring: a badge, an inline alert, a banner on a partial load. 5.10:1 on ground. Dark amber rather than yellow because no yellow clears 4.5:1 on a light ground. |
| `--hw-warning-quiet` | `oklch(0.944 0.035 78)` | `oklch(0.25 0.035 78)` | Fill behind warning text in a badge or banner. Solved so hw-warning clears AA on it, which the first pass asserted but never checked. |
| `--hw-danger` | `oklch(0.539 0.13 27)` | `oklch(0.643 0.11 27)` | Failed, revoked, destructive: a badge, a field error border, a destructive menu item, a negative delta. 5.09:1 on ground. |
| `--hw-danger-quiet` | `oklch(0.947 0.023 27)` | `oklch(0.253 0.035 27)` | Fill behind danger text in a badge or banner. Solved so hw-danger clears AA on it, which the first pass asserted but never checked. |
| `--hw-chart-1` | `oklch(0.545 0.085 198)` | `oklch(0.66 0.1 198)` | First data series. Shares the accent hue so the primary series reads as the subject. |
| `--hw-chart-2` | `oklch(0.545 0.115 268)` | `oklch(0.66 0.1 268)` | Second data series, in bars, lines, areas and their direct labels. Never a state. |
| `--hw-chart-3` | `oklch(0.545 0.115 328)` | `oklch(0.66 0.1 328)` | Third data series, in bars, lines, areas and their direct labels. Never a state. |
| `--hw-chart-4` | `oklch(0.545 0.115 28)` | `oklch(0.66 0.1 28)` | Fourth data series, in bars, lines, areas and their direct labels. Never a state. |
| `--hw-chart-5` | `oklch(0.545 0.103 88)` | `oklch(0.66 0.1 88)` | Fifth data series, in bars, lines, areas and their direct labels. Never a state. |
| `--hw-chart-6` | `oklch(0.545 0.115 148)` | `oklch(0.66 0.1 148)` | Sixth data series. Six is the limit; past that, group the tail. |
| `--hw-scrim` | `rgba(9, 12, 12, 0.45)` | `rgba(0, 0, 0, 0.65)` | Behind a dialog only. Nothing else dims the page. |

### spacing

| token | value | role |
|---|---|---|
| `--hw-space-2` | `2px` | The badge's vertical inset, and nothing else. At 4px a micro badge stands taller than the label beside it. The icon-to-label gap is --hw-icon-gap at 6px; this token claimed that job too and did not have it. |
| `--hw-space-4` | `4px` | Inside a badge or a chip; the gap between a label and its control. |
| `--hw-space-8` | `8px` | Between controls in a row; padding inside a dense table cell. |
| `--hw-space-12` | `12px` | Padding inside a button or input; the gap between list rows. |
| `--hw-space-16` | `16px` | Padding inside a card; the gap between cards. |
| `--hw-space-24` | `24px` | Between a heading and its content; padding in a dialog. |
| `--hw-space-32` | `32px` | Padding at a screen's edge; between sections of a form. |
| `--hw-space-48` | `48px` | Between major sections of a page. |
| `--hw-space-64` | `64px` | Above and below a page heading on a marketing surface. |
| `--hw-space-96` | `96px` | Between bands on a marketing surface. The largest step; nothing is bigger. |

### radius

| token | value | role |
|---|---|---|
| `--hw-radius-sm` | `4px` | Things inside other things: badges, checkboxes, swatches, the status dot. |
| `--hw-radius-md` | `6px` | Interactive controls: buttons, inputs, selects, menu items, tabs. |
| `--hw-radius-lg` | `10px` | Containers: cards, panels, dialogs, popovers, toasts. |
| `--hw-radius-full` | `9999px` | Avatars and count pills only, where the shape itself carries the meaning. |

### shadow

| token | value | role |
|---|---|---|
| `--hw-shadow-overlay` | `0 1px 2px rgba(9, 12, 12, 0.06), 0 8px 24px -4px rgba(9, 12, 12, 0.10)` | Popovers, menus and toasts in light theme. Dark theme carries elevation by surface lightness instead. |
| `--hw-shadow-dialog` | `0 2px 4px rgba(9, 12, 12, 0.07), 0 24px 56px -12px rgba(9, 12, 12, 0.18)` | Dialogs and sheets in light theme, the only element allowed a shadow this large, and a menu-bar panel in both themes (36-form-factors.md). |

### duration

| token | value | role |
|---|---|---|
| `--hw-duration-instant` | `90ms` | A state change the user is already looking at: a checkbox tick, a switch knob, a segmented-control fill. |
| `--hw-duration-fast` | `150ms` | The default for every transition: hover fills, focus rings, border colour, disclosure. 0.15s is the most-used duration across the reference set. |
| `--hw-duration-base` | `220ms` | Something entering or leaving the layer stack: a popover, a menu, a toast. |
| `--hw-duration-slow` | `380ms` | A dialog or a sheet, where the scrim and the panel move together and the user must follow both. |

### easing

| token | value | role |
|---|---|---|
| `--hw-ease-out` | `cubic-bezier(0.22, 1, 0.36, 1)` | The default, for anything appearing or responding to a press: fast at the start, settled at the end. |
| `--hw-ease-in` | `cubic-bezier(0.4, 0, 1, 1)` | Only for something leaving the screen, where the exit should not compete for attention. |
| `--hw-ease-standard` | `cubic-bezier(0.4, 0, 0.2, 1)` | A value moving between two on-screen positions, such as a slider thumb or a progress fill. |

### layout

| token | value | role |
|---|---|---|
| `--hw-bp-sm` | `600px` | Phone to small tablet. Below it a screen is one column and the rail is a sheet. |
| `--hw-bp-md` | `768px` | The measured consensus edge: 23 of 44 captured sites declare a 768px min-width, more than any other value. |
| `--hw-bp-lg` | `1024px` | Second most declared, on 18 of 44. The rail becomes permanent here. |
| `--hw-bp-xl` | `1280px` | Declared on 12 of 44. The content column stops growing; the margins take the rest. |
| `--hw-bp-2xl` | `1600px` | Only a wide table or a three-pane shell uses the extra width. Prose never does. |
| `--hw-gutter-sm` | `16px` | Page margin below hw-bp-md. Measured at 390px wide: 16px on 6 of 12 sites, 24px on 3. |
| `--hw-gutter-md` | `24px` | Page margin from hw-bp-md to hw-bp-lg. |
| `--hw-gutter-lg` | `32px` | Page margin at hw-bp-lg and above. |
| `--hw-column-gap` | `24px` | The gutter between grid columns. 24px is the fourth most used column-gap in the capture (93 uses) and the largest that stays on the 4px grid at every step. |
| `--hw-columns` | `12` | Twelve columns above hw-bp-lg, six from hw-bp-md, four below. 58 of the grids sampled use twelve. |
| `--hw-container-app` | `1440px` | The app shell's cap. Past it the content pane stops and the window grows around it. |
| `--hw-container-page` | `1200px` | Marketing and documentation. The 1100-1300 band is the most occupied in the capture: 20 of 44 sites. |
| `--hw-container-prose` | `720px` | A reading column on its own. The median measured prose column at 1440px across the capture is 721px. |
| `--hw-measure-ui` | `56ch` | Running text inside product UI. |
| `--hw-measure-prose` | `68ch` | Long-form. The median measured running-text width across 30 sites is 64ch; 68ch sits inside the comfortable band and above the product step. |
| `--hw-rail` | `248px` | The app shell's navigation rail at hw-bp-lg and above. |
| `--hw-rail-collapsed` | `56px` | The rail reduced to icons, which is the only collapsed state it has. |
| `--hw-panel-w` | `352px` | A menu-bar utility panel. 40ch of body-sm Public Sans measures 318.4px with the face loaded, plus hw-cell-pad-x each side, rounded up to the 4px grid. |
| `--hw-panel-max-h` | `464px` | A menu-bar panel's ceiling: a 32px header, 12 rows at hw-row-h-compact and a 48px footer. Always clamped further by min(, calc(100vh - hw-space-48)). |

### density

| token | value | role |
|---|---|---|
| `--hw-control-h-sm` | `24px` | Compact control height: a toolbar button, an inline filter. |
| `--hw-control-h` | `32px` | The default control height. 32px is the most common measured button height in the capture, on 14 of 44 sites. |
| `--hw-control-h-lg` | `40px` | A single call to action on a marketing surface. Second most measured, on 11 sites. |
| `--hw-row-h` | `44px` | Comfortable table or list row: 13px text on 1.5 leading plus 12px above and below. |
| `--hw-row-h-compact` | `32px` | Compact row. The same text, 6px above and below, and nothing else changes. |
| `--hw-cell-pad-x` | `16px` | Horizontal cell padding, both densities. Density changes the vertical rhythm, never the column alignment. |
| `--hw-cell-pad-y` | `12px` | Vertical cell padding, comfortable. |
| `--hw-cell-pad-y-compact` | `6px` | Vertical padding in a table cell and a list row under data-density="compact". Horizontal padding does not change, which is what keeps a column in the same place across the toggle. |
| `--hw-field-pad-x` | `12px` | Horizontal padding inside a field, a select, a stepper and a search box. Unchanged by density, like hw-cell-pad-x, so a column of controls keeps its left edge across the toggle. Was an unnamed 10px literal in the Input anatomy, off the 4px grid. |
| `--hw-field-pad-y` | `7px` | Input vertical padding, comfortable. |
| `--hw-field-pad-y-compact` | `3px` | Input vertical padding, compact. |

### icon

| token | value | role |
|---|---|---|
| `--hw-icon-sm` | `14px` | Inside a badge or beside 12px label text. |
| `--hw-icon` | `16px` | The default. 16x16 is the most widely used rendered icon size in the capture: 21 of 44 sites. |
| `--hw-icon-lg` | `20px` | A toolbar or rail icon that carries its own hit target. |
| `--hw-icon-stroke` | `1.5px` | Stroke weight at 16px and 20px. 1.5px on 8 sites, 2px on 12; 1.5px is the lighter of the two and the one that survives beside 13px text. |
| `--hw-icon-gap` | `6px` | Icon to its own label. Below the 4px grid on purpose: 4px reads as one object, 8px as two. |

### zIndex

| token | value | role |
|---|---|---|
| `--hw-z-base` | `0` | Page content. |
| `--hw-z-sticky` | `100` | A sticky table head or a pinned toolbar. |
| `--hw-z-rail` | `200` | The app shell's rail when it overlays content below hw-bp-lg. |
| `--hw-z-dropdown` | `300` | Menu, select, popover, tooltip. |
| `--hw-z-scrim` | `400` | The dialog scrim. |
| `--hw-z-dialog` | `500` | The dialog itself. |
| `--hw-z-toast` | `600` | Toasts, which must clear a dialog because they report what the dialog did. |

### stroke

| token | value | role |
|---|---|---|
| `--hw-border-w` | `1px` | Every border in the system. There is no 2px border. |
| `--hw-focus-w` | `2px` | The focus ring. 2px is the most used real outline-width in the capture: 246 declarations against 75 at 1px. |
| `--hw-focus-offset` | `2px` | The gap between the control's own edge and its ring, so the ring reads as a ring. |
| `--hw-tab-active-w` | `2px` | The rule under the selected tab, which is the one place a 2px line is a state. |

### type

| step | face | size | line-height | tracking | weight | role |
|---|---|---|---|---|---|---|
| `display-1` | display | 56px | 1.02 | -0.022em | 500 | Page-defining headline on a marketing or release surface. One per page. |
| `display-2` | display | 40px | 1.08 | -0.020em | 500 | Section opener on a marketing surface. |
| `title-1` | sans | 28px | 1.15 | -0.018em | 600 | The screen's own name. One per screen. |
| `title-2` | sans | 21px | 1.25 | -0.012em | 600 | A section inside a screen. |
| `title-3` | sans | 17px | 1.35 | -0.006em | 600 | A card header or a form group heading. |
| `body-lg` | sans | 17px | 1.6 | 0em | 400 | Long-form running text in docs and release notes. Measure 68ch. |
| `body` | sans | 15px | 1.55 | 0em | 400 | The default for product UI. Measure 56ch. |
| `body-sm` | sans | 13px | 1.5 | 0em | 400 | Dense UI: table cells, list rows, control labels, help text. |
| `label` | sans | 12px | 1.35 | 0.01em | 500 | A control's own label, a hint, a badge. |
| `micro` | sans | 11px | 1.2 | 0.06em | 600 | Column heads and eyebrows, set uppercase. Nothing smaller exists. |

## Faces

- **sans**: `"Public Sans", "Libre Franklin", -apple-system, BlinkMacSystemFont, "Segoe UI", Roboto, "Helvetica Neue", Arial, sans-serif`
- **display**: `"Newsreader", "Iowan Old Style", "Source Serif 4", Georgia, "Times New Roman", serif`
- **mono**: `"IBM Plex Mono", ui-monospace, "SF Mono", "JetBrains Mono", Menlo, Consolas, monospace`

## What this file does not carry

The reasoning, the measurements every value was solved against, the component anatomies, the anti-pattern list and the spec sheet of named assets. Those are the numbered files beside this one; `README.md` says which answers what.
